#include<bits/stdc++.h>
using namespace std;

int main(){

     int n;  // All those are single task that will take step  
     cin >> n;
     int arr[n];

     for (int i = 0; i < n; i++)  // It will take O(n) timecompelxitiy
     {
        cin >> arr[i];
     }


    //  if there is n * 2 or
    //  if there is n -  2 or
    //  if there is n / 2 or
    //  if there is n % 2 or
    //  if there is n anything else  2 or

    // we will count O(N);
     

    return 0;
}