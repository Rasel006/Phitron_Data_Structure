#include<bits/stdc++.h>
using namespace std;

int main(){
    // Those all use the consturcor 
    //  First way or type how to intialize vector 
    // vector <int> v;

    // Second way or type how to initialize vector 
    // vector <int> v(5);

    // Therd way or type how intilaize vector and give and own value 
    // vector <int> v(5,20);

    // Forth way or type how copy another vector 
    // vector <int> ve(5, 10); // for copy
    // vector <int> v(ve); // copy the ve

    // Fifth way or type how copy value form an array 
    // int arr[5] = {10, 20, 30, 40, 50};
    // vector <int> v(arr, arr + 5);

    // Without consturtor 
    // vector <int> (v) ={ 10, 20, 30, 40, 50};      

    // Print the vector or the size of the vector
    // for (int i = 0; i < 5; i++)
    // {
    //     cout << v[i]<< " ";
    // }

    // cout << endl << v.size();

    return 0;
}